local ReplicatedStorage = game:GetService("ReplicatedStorage")

local AudioController = require(script.Audio.AudioController)
local MusicController = require(script.Audio.MusicController)
local GameplayCamera = require(script.Camera.GameplayCamera)
local FighterController = require(script.Character.FighterController)
local MeleePresentationController = require(script.Combat.MeleePresentationController)
local FrontendController = require(script.Frontend.FrontendController)
local InputManager = require(script.Input.InputManager)
local EnvironmentQualityController = require(script.Maps.EnvironmentQualityController)
local DashPresentationController = require(script.Movement.DashPresentationController)
local TechniqueRhythmController = require(script.Movement.TechniqueRhythmController)
local WallInteractionController = require(script.Movement.WallInteractionController)
local HUDController = require(script.UI.HUDController)
local InputOverlayController = require(script.UI.InputOverlayController)
local ScoreboardController = require(script.UI.ScoreboardController)
local SettingsController = require(script.UI.SettingsController)
local ChatController = require(script.UI.ChatController)
local NavigationController = require(script.Frontend.NavigationController)

local Shared = ReplicatedStorage:WaitForChild("Shared")
local MovementEngine = require(Shared.Movement.MovementEngine)

InputManager:Init()
EnvironmentQualityController:Init()
FighterController:Init()
GameplayCamera:Init(InputManager)
MovementEngine:Init(InputManager, GameplayCamera)
DashPresentationController:Init(MovementEngine, GameplayCamera)
WallInteractionController:Init(InputManager, MovementEngine, GameplayCamera)
MeleePresentationController:Init(InputManager, FighterController, MovementEngine)
TechniqueRhythmController:Init(InputManager, MovementEngine, MeleePresentationController, WallInteractionController, FighterController)
AudioController:Init()
MusicController:Init(NavigationController)
HUDController:Init(FighterController)
InputOverlayController:Init(InputManager)
ScoreboardController:Init(InputManager)
SettingsController:Init(InputManager, GameplayCamera)
ChatController:Init(InputManager)
FrontendController:Init(InputManager, GameplayCamera, FighterController, SettingsController, MusicController, ChatController)
